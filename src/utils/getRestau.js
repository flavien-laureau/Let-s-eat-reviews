import store from '../utils/restauStore';

export default function getRestau(){
    /* const service = new google.maps.places.PlacesService(store.state.map);

    const getDetailsRequest = [{
        fields: ['all'],
        placeId: 'ChIJa_T_-nCu8kcRkINf0iMAXY8'
    }]

    function getDetailsCallback(results, status){
        if (status == google.maps.places.PlacesServiceStatus.OK){
            console.log(results)
        }
    }

    service.getDetails(getDetailsRequest[0], getDetailsCallback) */


}



